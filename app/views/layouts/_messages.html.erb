<% flash.each do |key, value| %>
	<!-- make use of bootstrap-sass class, pass key for overrides -->
  <div class="alert alert-info <%= key %>">
		<!-- <a href="#" data-dismiss="alert" class="close">×</a> -->
		<%= value %>
  </div>
<% end %>
